# UI Cleanup Notes

This version keeps the data pipeline, ML logic, risk scoring, forecasting, weather context, and geospatial logic unchanged.

UI fixes applied:
- Removed boxed sidebar buttons; navigation is now a clean dark vertical menu.
- Hid Streamlit raw sidebar collapse control that appeared as `keyboard_double...`.
- Fixed duplicated/overlapping file-uploader button text.
- Converted the workflow card into a subtle checklist instead of a bulky box.
- Kept professional OSHEE operations layout and TailAdmin-style main content.
